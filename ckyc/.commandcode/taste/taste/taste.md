# Taste
- Wants report deliverables to be maintained as living status-tracking documents: each finding marked done/not-done (✅/⏳), with dates/commit context and a clear "what's left" section, so reports stay current after code changes are made. Confidence: 0.8
- Wants performance/optimization reports to include a quantified speedup/impact estimate after changes (e.g. "≈3–8× on the SQLite-portion of write-heavy stages"), with a clear caveat distinguishing an estimate from an actual benchmark. Confidence: 0.8
- Explicitly instructs "don't make any changes until told so" — performs analysis and writes report deliverables but does NOT modify source code until given explicit approval. Confidence: 0.9
- Explicitly instructs "don't make any changes until told so" — performs analysis and writes report deliverables but does NOT modify source code until given explicit approval. Confidence: 0.9
- When authorizing code changes, demands "strictly no regression" — prefers behavior-preserving optimizations (single-transaction/single-statement rewrites, non-unique indexes, dictionary lookups) and verification (build, spec checks, baseline comparison) over riskier refactors or schema constraints that could break existing data/behavior. Confidence: 0.85
- Values genuine async/await correctness: no sync-over-async (`.Result`/`.Wait()`/`GetAwaiter().GetResult()`), no `async void`, no blocking calls; expects proper `await` + disposal, and verifies it (e.g. by grepping the tree for async anti-patterns). Confidence: 0.7
- Prefers comparative analysis/deliverables presented as side-by-side tables, with options compared across multiple dimensions (e.g. functionality AND performance) rather than prose. Confidence: 0.85
- When evaluating third-party library/dependency options, wants the license of each candidate listed as part of the comparison (e.g. "list the license for each"). Confidence: 0.75
- When evaluating third-party dependency/library options for a sensitive/deployed environment, wants candidates ranked on stability, security (CVE/dependency footprint/supply-chain risk), and active maintenance (LTS/patch channel, maintainer backing), not just raw performance. Confidence: 0.8
- Prefers proper structured logging over raw `Console.WriteLine`: named message-template parameters captured as queryable JSON properties (e.g. `{CustomerId}`, `{BatchKey}`), with severity mapping (Info for progress, Warn for skipped/rejected, Error for failures) and a separate internal self-diagnostics log that never pollutes the business log. Confidence: 0.65
